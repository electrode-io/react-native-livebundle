# JavaScript bundler
bundler:
  metro:
    bundles:
      - dev: true
        entry: index.js
        platform: ios
      - dev: false
        entry: index.js
        platform: ios

# React Native server
server:
  metro:

# Storage
storage:
  fs:
    storageDir: ~/.livebundle/storage

# Generators
generators:
  qrcode:
  deeplink:

# Notifiers
notifiers:
  terminal:
  viewer:
