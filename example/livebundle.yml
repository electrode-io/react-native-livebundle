# JavaScript bundler
bundler:
  metro:
    bundles:
      - dev: true
        entry: index.js
        platform: ios
      - dev: true
        entry: index.js
        platform: android

# React Native server
server:
  metro:

# Storage
storage:
  fs:
    storageDir: ~/.livebundle/storage

# Generators
generators:
  qrcode:
  deeplink:
    host: io.livebundle.example

# Notifiers
notifiers:
  terminal:
  viewer:
