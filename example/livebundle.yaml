# JavaScript bundler
bundler:
  metro:
    bundles:
      - dev: true
        entry: index.js
        platform: android
      - dev: true
        entry: index.js
        platform: ios

# Storage
storage:
  fs:
    storageDir: ~/.livebundle/storage

# Generators
generators:
  qrcode:
  deeplink:

# Notifiers
notifiers:
  terminal:
  viewer:
