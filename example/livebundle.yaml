# JavaScript bundler
bundler:
  metro:
    bundles:
      - dev: true
        entry: index.js
        platform: android

# Storage
storage:
  fs:
    storageDir: ~/.livebundle/storage

# Generators
generators:
  qrcode:
  deeplink:

# Notifiers
notifiers:
  terminal:
  viewer:
